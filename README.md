# Deject
🚀 Blazingly fast Windows DLL injector written in Rust

> [!CAUTION]
> This is the development branch. Code here is not suitable for production.

# Building
> [!NOTE]
> Precompiled binaries are currently unavailable. Please compile from source.

- Install Rust from [rustup.rs](https://rustup.rs) if not installed.
- Clone the repository and enter the folder with `git clone https://github.com/Deltara3/Deject && cd Deject`.
- For a debug build, run `cargo build`. For a release build, run `cargo build --release`.
- Once done, the executable will be located in `target\debug` or `target\release` depending on configuration.

# Usage
> [!NOTE]
> Will be added after the project is functional.

# License
All code is licensed under the MIT License. Ubuntu Sans Mono is distributed under the Ubuntu Font License.
